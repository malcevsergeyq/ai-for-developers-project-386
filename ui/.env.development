# Во время разработки фронт ходит в мок Prism, поднятый по контракту:
# в корне проекта — npm run mock
# Адрес именно 127.0.0.1, а не localhost: Prism слушает IPv4, и на Windows
# localhost может отрезолвиться в ::1 — тогда запросы уйдут в никуда.
VITE_API_URL=http://127.0.0.1:4010
